function* containers(
  first = alpha ? beta : gamma,
  second = left && right,
) {
  for (let index = start && end; ready && set; index = next && final) {}
  for (const key in source && fallback) {}
  for (const value of source && fallback) {}
  const values = [...(spread && rest), { [key && name]: `${tag && value}` }];
  const made = new Factory(member?.[lookup && key], import(module && name));
  yield wrapped && value;
  return <View {...(props && more)}>{one && two && <Child value={nested ? yes : no} />}</View>;
}

function classOwner() {
  class Local {
    field = fieldLeft && fieldRight;
    static {
      if (staticCondition) {}
    }
    method() {
      if (methodCondition) {}
    }
  }
  return Local;
}

function enumOwner() {
  enum Local {
    Entry = enumLeft && enumRight,
  }
  return Local;
}
